#include <kvs/TextCommandHandler.hpp>

namespace kvs {

bool TextCommandHandler::dispatch()
{
  // do
    // read fd -> buffer
    // if whole line found
      // process line
    // else
  // while readSize == bufferSize
  return _fd > 0;
}

} // namespace
